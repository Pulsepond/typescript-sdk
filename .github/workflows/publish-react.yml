name: Publish React

on:
  release:
    types:
      - published

permissions:
  contents: read
  id-token: write

concurrency:
  group: npm-publish-react
  cancel-in-progress: false

jobs:
  publish:
    if: ${{ !github.event.release.prerelease && startsWith(github.event.release.tag_name, 'react-v') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6
        with:
          version: 10.34.5
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          package-manager-cache: false
          registry-url: https://registry.npmjs.org
      - name: Pin npm
        run: npm install --global npm@11.18.0
      - name: Verify release source
        run: |
          git fetch --no-tags origin main
          git merge-base --is-ancestor "$GITHUB_SHA" FETCH_HEAD
      - name: Verify release tag
        run: node packages/react/scripts/check-release.mjs
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Validate
        run: pnpm check
      - name: Pack publishable workspace package
        id: package
        run: |
          version="$(node -p "require('./packages/react/package.json').version")"
          pnpm --dir packages/react pack --pack-destination "$RUNNER_TEMP"
          echo "archive=$RUNNER_TEMP/pulsepond-react-$version.tgz" >> "$GITHUB_OUTPUT"
      - name: Publish to npm
        run: npm publish "${{ steps.package.outputs.archive }}" --access public --provenance
