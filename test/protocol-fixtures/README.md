# Protocol fixture snapshot

These client-facing fixtures are copied from Pulsepond core protocol v1 at
commit `4f4fb0a57af68615d47d5c37462df7fa115098ce`.

They are test-only: Ajv must never enter the browser package. When protocol v1
changes, update this complete snapshot and the SDK together.
